import {StyleSheet} from 'react-native';
import {colors} from 'src/themes/colors';

export const styles = StyleSheet.create({
  container: {flex: 1, paddingTop: 40, backgroundColor: colors.light},
});
